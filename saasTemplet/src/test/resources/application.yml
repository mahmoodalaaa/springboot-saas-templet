spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-placeholder.auth0.com/
          audiences: api://placeholder

polar:
  access-token: mock-token
  webhook-secret: mock-secret
